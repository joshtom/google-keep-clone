/**
 * This is an Example component and all component should follow this structure
 * @param { props } -> Props can be destructured like this
 * @package { prop-types }: Usage of prop types is also allowed to validate all input props
 */

const ExampleComponent = () => {
    return (
        <h1> Example Component </h1>
    )
}

export default ExampleComponent;